# ============================================================
# Shiny App - IFLS5 Risk Preference Predictor
# ============================================================

library(shiny)

ui <- fluidPage(
  titlePanel("Risk Preference Predictor (IFLS5)"),
  sidebarLayout(
    sidebarPanel(
      h4("Input Your Big Five Scores"),
      # TODO: sliders for O, C, E, A, N + demographics
    ),
    mainPanel(
      h4("Predicted Risk Preference"),
      # TODO: prediction output + distribution plot
    )
  )
)

server <- function(input, output, session) {
  # TODO: load model, predict, render
}

shinyApp(ui, server)